name: Release p2 (GitHub Pages)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: CI release is disabled
        run: |
          echo "Automated CI release/publish is disabled."
          echo "Use local script: tools/publish-p2-local.sh"
