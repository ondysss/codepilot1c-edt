name: Build

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: CI build is disabled
        run: |
          echo "Automated CI build/publish is disabled."
          echo "Use local script: tools/publish-p2-local.sh"
